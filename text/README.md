# Flookup-Invoice-Reader

- First use pip install -r requirements.txt to install dependancies
- Run run.py to start Fast API
- /scan will take a post request containing PDF and parse relevant information from it
